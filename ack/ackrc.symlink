# Include column number of the first match on each line
--column

# Follow symlinks
--follow

# Use less for paging results
--pager=less -r

# Ignore case unless the pattern is mixed case
--smart-case

# Additional types to search
--type-add
css=.less

--type-set
asp=.asp

